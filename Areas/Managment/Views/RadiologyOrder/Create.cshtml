@model Clinc_Care_MVC_Grad_PROJ.Areas.Managment.Models.RadiologyOrder

@{
    ViewData["Title"] = "Create";
}

<h1>Create Laborder</h1>

<h4>RadiologyOrder</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AppointmentId" class="control-label"></label>
                <select asp-for="AppointmentId" class="form-control" asp-items="ViewBag.Appointmentlst"></select>
            </div>
            <div class="form-group">
                <label asp-for="RadiologyTypeId" class="control-label"></label>
                <select asp-for="RadiologyTypeId" class="form-control" asp-items="ViewBag.Radiologytypelst"></select>
            </div>
            <div class="form-group">
                <input asp-for="IsInternal">
                <label asp-for="IsInternal" class="control-label"></label>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
